exports.getResult = function(curr_page_num,avg_num,result,callback){	if(curr_page_num==null)		curr_page_num = 1; //默认当前页为第1页	if(avg_num==null)		avg_num = 8;	//默认每页8个	if(result&&result.length==0){		callback(null,result);		return;	}	if(result.length<(curr_page_num-1)*avg_num){//没有第curr_page_num页。		callback('result have no so many pages');		return;	}	var pageLastIndex = curr_page_num*avg_num;	if(pageLastIndex>result.length)		pageLastIndex = result.length;	var pageFirstIndex = (curr_page_num-1)*avg_num  ;	callback(null,result.splice(pageFirstIndex,avg_num));};